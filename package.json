{
  "name": "olink-desk",
  "private": true,
  "version": "0.1.0",
  "description": "Olink Desk — customer support, task & call tracking for Ethiopian organizations",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
